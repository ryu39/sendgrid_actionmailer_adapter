language: ruby

rvm:
  - head
  - 2.4.1
  - 2.3.4

sudo: false

before_install:
  - 'gem install bundler --no-document'
script:
  - 'bundle exec rspec'
after_success:
  - 'bundle exec codeclimate-test-reporter'

cache: bundler
